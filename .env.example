# Backend default: http://localhost:8080 (see CrewForge api_module)
# For Vite dev: use the proxy and point the app at /api (see README)
VITE_API_BASE_URL=/api

# Direct to Flask (no proxy) — also valid:
# VITE_API_BASE_URL=http://localhost:8080
